# langchain-mcp-sql-agent
Agentic text-to-SQL assistant: a LangChain agent on a local Qwen3 model discovers a database schema over MCP, writes its own SQL, runs it read-only, and charts the result in Streamlit.
